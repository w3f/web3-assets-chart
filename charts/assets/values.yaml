environment: production
replicaCount: 1

image:
  repository: web3f/assets
  version: v0.0.1
  pullPolicy: IfNotPresent

rclone:
  config:

serviceAccount:
  create: true

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

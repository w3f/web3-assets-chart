apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web3-assets.fullname" . }}-test-connection"
  labels:
    {{- include "web3-assets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web3-assets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "web3-assets.fullname" . }}
  labels:
    {{- include "web3-assets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web3-assets.selectorLabels" . | nindent 4 }}
